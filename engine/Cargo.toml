[package]
name = "hive-chess"
version = "0.3.0"
edition = "2021"

[dependencies]
chess = "3.2"
binread = { version = "2", features = ["const_generics"] }

[profile.release]
opt-level = 3
lto = true
strip = true
